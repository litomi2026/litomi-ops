apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: litomi-recommender-env
spec:
  refreshInterval: 15m
  secretStoreRef:
    kind: ClusterSecretStore
    name: oci-vault
  target:
    name: litomi-recommender-env
    creationPolicy: Owner
    deletionPolicy: Delete
  dataFrom:
    - extract:
        key: litomi-prod-recommender
