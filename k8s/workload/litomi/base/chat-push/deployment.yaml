apiVersion: apps/v1
kind: Deployment
metadata:
  name: litomi-chat-push
  labels:
    app.kubernetes.io/name: litomi-chat-push
    app.kubernetes.io/part-of: litomi
spec:
  replicas: 1
  minReadySeconds: 5
  progressDeadlineSeconds: 600
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: litomi-chat-push
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: litomi-chat-push
        app.kubernetes.io/part-of: litomi
    spec:
      automountServiceAccountToken: false
      serviceAccountName: litomi-chat-push
      priorityClassName: workload-standard
      terminationGracePeriodSeconds: 30
      nodeSelector:
        node-pool-role: workload
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/name: litomi-chat-push
          maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
      containers:
        - name: chat-push
          image: ghcr.io/litomi2026/litomi-chat-push:overlay-required
          imagePullPolicy: IfNotPresent
          env:
            # Serves no traffic; this port exists only for kubelet probes.
            - name: PORT
              value: '3004'
            - name: K8S_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          envFrom:
            - configMapRef:
                name: litomi-chat-push-env
            - secretRef:
                name: litomi-chat-push-env
          ports:
            - containerPort: 3004
              name: http
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12
          # Readiness mirrors the drain flag: once shutting down, /health returns
          # 503. No Service, so this mainly gates rollout progress.
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          # Liveness is a bare TCP check on purpose: /health 503s while draining,
          # so an httpGet liveness probe would restart a healthy pod mid-drain.
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
