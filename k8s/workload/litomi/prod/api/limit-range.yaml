apiVersion: v1
kind: LimitRange
metadata:
  name: defaults
spec:
  limits:
    - default:
        cpu: 1200m
        memory: 1536Mi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      type: Container
