apiVersion: v1
kind: LimitRange
metadata:
  name: defaults
spec:
  limits:
    - default:
        cpu: 1000m
        memory: 512Mi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      type: Container
